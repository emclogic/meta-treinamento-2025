header:
  version: 16

target: treinamento-bsp
build_system: oe
distro: poky

repos:
  meta-treinamento-2025:

local_conf_header:
  rm_work: |
    INHERIT += "rm_work"
  package_management: |
    PACKAGE_CLASSES = "package_ipk"
  debug-tweaks: |
    EXTRA_IMAGE_FEATURES += "debug-tweaks allow-empty-password empty-root-password"